# Dependências da API em produção.
#
# O `torch` não está listado aqui: ele é instalado no Dockerfile a partir
# do índice CPU-only da PyTorch. A versão do PyPI inclui bibliotecas CUDA,
# ultrapassa 2 GB e pode exceder os limites de qualquer free tier.
#
# Instalação equivalente fora do Docker:
#
#     pip install torch \
#         --index-url https://download.pytorch.org/whl/cpu
#
# `pandas` é uma dependência real de runtime, e não apenas de treinamento:
# `app/features.py` o importa, e `app/schemas.py` importa `app/features`.
# Removê-lo deste arquivo faria a imagem da API falhar com
# `ModuleNotFoundError`.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
numpy==2.2.1
pandas==2.2.3
