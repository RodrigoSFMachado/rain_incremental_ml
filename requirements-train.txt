# Dependências de treino, experimentação, testes e dashboard local.
# Não entram na imagem da API.
#
# pandas e numpy já vêm de requirements.txt via -r, por isso não são
# repetidos aqui.

-r requirements.txt

pyarrow==18.1.0       # leitura/escrita do dataset.parquet
matplotlib==3.10.0    # gráficos do training/experiment.py
mlflow==3.15.1        # tracking de experimentos (offline)
pytest==8.3.4
httpx==0.28.1         # cliente da simulação + TestClient do FastAPI
streamlit==1.41.1     # dashboard rodando fora do Docker
